-- Create a new database called salesDB (only if it doesn’t already exist)
CREATE DATABASE IF NOT EXISTS salesDB;

-- Drop (delete) the database called demo if it exists
DROP DATABASE IF EXISTS demo;
